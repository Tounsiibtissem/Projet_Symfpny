{% extends 'base.html.twig' %}

{% block title %}Marque{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ marque.id }}</td>
            </tr>
            <tr>
                <th>Nom_marque</th>
                <td>{{ marque.nomMarque }}</td>
            </tr>
              <tr>
                <th>action</th>
                <td>                    
                <a href="{{ path('app_marque_index') }}">back to list</a>
                <a href="{{ path('app_marque_edit', {'id': product.id}) }}">edit</a></td>
        </tbody>
        </tbody>
    </table>

    {{ include('marque/_delete_form.html.twig') }}
{% endblock %}
