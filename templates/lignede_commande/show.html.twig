{% extends 'base.html.twig' %}

{% block title %}LignedeCommande{% endblock %}

{% block body %}
    <h1>LignedeCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lignede_commande.id }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ lignede_commande.quantité }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_lignede_commande_index') }}">back to list</a>

    <a href="{{ path('app_lignede_commande_edit', {'id': lignede_commande.id}) }}">edit</a>

    {{ include('lignede_commande/_delete_form.html.twig') }}
{% endblock %}
